// initialisation
window.onload = load;
// system functions
function load() {
    initialise();
}
function initialise() {
    // and then we begin
}
var Table = /** @class */ (function () {
    function Table(width, height) {
        this.width = width;
        this.height = height;
        // what goes here?
    }
    return Table;
}());
var Game = /** @class */ (function () {
    function Game(type) {
        this.type = type;
        // what goes here?
    }
    return Game;
}());
var Layout = /** @class */ (function () {
    function Layout() {
    }
    return Layout;
}());
