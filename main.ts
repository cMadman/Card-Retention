// initialisation
window.onload = load;

// system functions
function load() {
    initialise();
}

function initialise() {
  // and then we begin
}

class Table {
    constructor(public width: number, public height: number) {
        // what goes here?
    }
}

class Game {
    constructor(public type: string) {
        // what goes here?
    }
}

class Layout {
    
}